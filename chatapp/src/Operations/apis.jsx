const BaseUrl = "http://localhost:8000/api/"

export const endPoints = {
    SIGNUP_API: BaseUrl + "auth/signup",
    LOGIN_API: BaseUrl + "auth/login",
}

export const messages = {
    ALLCONVERSATION: BaseUrl + "conversation",
}